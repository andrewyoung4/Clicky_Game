import React from 'react';
import './Banner.css';

const Banner = () => (
  <div className="banner text-center d-flex align-items-center justify-content-center">
    <p className="m-0">Click on an image to earn points, the name of the game is to remember each image clicked and only click each image once!</p>
  </div>
);

export default Banner;
